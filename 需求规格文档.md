# MeWork · 需求规格文档（PRD）

> 本文档是新项目「MeWork」的完整需求规格与架构决策基准。开发严格依据本文档推进；架构变更须先与用户确认、升版本号并登记修改记录。
> 配套：《开发计划.md》（阶段里程碑）、《进度看板.md》（单一事实源）、《CLAUDE.md》（AI 助手工作流）。

| 版本 | 日期 | 修改记录 |
|---|---|---|
| v0.1 | 2026-08-08 | 初始需求确认稿（逐行确认中） |
| v1.0 | 2026-08-08 | 逐行确认完成，定稿（产品名定为 MeWork） |
| v1.1 | 2026-08-08 | 评审修订：A1 笔误 / A2 日程彻底砍掉 / A3 保留策略确定性规则 / A4 设置补内容 / B1–B5 边界定案 / C1–C9 措辞 |
| v1.2 | 2026-08-09 | 阶段 2.6 定案：`.wr/` 衍生目录命名定案（§3.3.2 / §8.2 关闭开放项 2） |

---

## 1. 项目概述

### 1.1 背景与定位

**1.1.1** 本产品是《WorkRecord》（工作记录 AI 管理工具，Electron + React + JS）的**极简重构版**：功能重做、部分沿用需求、架构重新设计。

**1.1.2** 与 WorkRecord 的功能取舍总览（详见 §6 需求覆盖核对表）：
- **沿用**：工作区设定、MD 编辑、目录树与文件管理、MD 预览、明暗主题、版本历史
- **砍掉**：全部 AI 功能（智能归档 / AI 整理 / AI 优化 / Agent 引擎 / 模型配置）、日程功能（计划 / 日程 / 提醒 / 文档模板）
- **新增**：标签页 / 多窗口
- **未来迭代**：AI 功能（本产品定位为「未来扩展项目」，当前版本不含 AI，架构预留扩展点，见 §7）

**1.1.3** 产品名：**MeWork**（2026-08-08 定名）；工作目录 / 仓库名沿用 RefatorMyRecord。

**1.1.4** 设计指引（全局原则，所有模块遵循）：
- **响应式布局**：系统 UI 需响应式——窗口缩放时布局自适应（侧边栏 / 分屏比例 / 编辑器 / 标签栏等合理适配，小窗口下可折叠收起）
- **工作区功能优化设计**：工作区相关功能需重新优化设计，**不参考 WorkRecord 既有实现**；具体优化方案在设计阶段确定，本文档不提前锁定（见 §8 开放问题）
- **模块高扩展性**：模块设计要求高扩展性、预留冗余——接口预留、能力抽象、避免写死，为未来扩展（含 AI）留足空间

### 1.2 核心价值

**1.2.1** 本地工作记录管理：在用户选定的真实文件夹（工作区）内创建、编辑、管理 Markdown / TXT 文档；文件真实落盘、可被其他编辑器直接打开。

**1.2.2** 可追溯：每次保存自动生成版本快照，支持行级 diff 对比、回滚、导出任意版本。

**1.2.3** 轻量纯粹：无 AI 依赖、无网络调用、全离线可用；启动快、资源占用低。

**1.2.4** 多任务并行：标签页支持同时打开多个文档，多窗口支持独立操作视图。

### 1.3 使用场景与用户

**1.3.1** 用户：单人（作者本人）本地自用。

**1.3.2** 形态：Windows 桌面应用，离线全功能可用。

**1.3.3** 典型场景：
- 在选定工作区中快速记录、编辑 MD 笔记 / 工作记录
- 编辑历史可追溯，误改可回滚
- 同时打开多个文档对比、并行编辑

---

## 2. 总体技术决策

| 编号 | 维度 | 决策 | 论证 |
|---|---|---|---|
| 2.1 | 技术栈 | **Electron + React + JavaScript**，构建工具 **electron-vite** | 与 WorkRecord 同栈，开发经验与代码可直接复用；Node fs 在主进程直用 |
| 2.2 | 未来 AI 扩展 | 选 Electron 的核心理由：「未来扩展 AI」是明确长期方向，WorkRecord 已验证的整套 Agent 架构（ReAct 工具循环 / HITL / 三层校验，全 JS）可直接移植（估 1–2 周）；若换 Tauri 需 Rust 重写（估 4–6 人月） | 决策 2.2 是选型核心，详见 §7 |
| 2.3 | 打包体积 | 砍掉全部 AI 依赖（langchain / deepagents / langgraph / langsmith 等）后，极简版 Windows NSIS 安装包预估 **45–65 MB**（WorkRecord 为 80–150 MB） | 体积在可接受范围；仅当「安装包 < 20 MB」为硬指标时才转向 Tauri |
| 2.4 | 文件归属 | **直接读写磁盘真实文件夹**，MD/TXT 真实落盘 | 文件可移植、可被其他编辑器直接打开 |
| 2.5 | 文件同步 | **单向**：应用写入即时生效；外部改动需手动刷新感知 | 遵循 WorkRecord 模型，不做复杂双向同步 |
| 2.6 | 进程模型 | fs / 版本管理 / 窗口在**主进程**；UI 在**渲染进程**；`contextIsolation:true`、`nodeIntegration:false`、`sandbox:true` | 安全底线，能力经 contextBridge 暴露 |
| 2.7 | 路径沙箱 | 所有文件读写限定在用户授权的工作区根目录内（pathGuard） | 安全底线，详见 §3.2 |
| 2.8 | 存储分层 | UI 偏好（主题 / 字体 / 最近工作区）走渲染进程 localStorage；敏感配置走主进程 safeStorage（当前无 API Key，预留） | 沿用 WorkRecord 分层原则 |
| 2.9 | AI | **当前版本不含 AI**（不引入任何 LLM 依赖），架构预留扩展点（§7） | 用户确认「先不考虑 AI，未来扩展」 |

---

## 3. 技术架构

### 3.1 Electron 进程划分

| 进程 | 职责 |
|---|---|
| 渲染进程（React UI） | 界面、编辑器、目录树、主题切换、版本对比展示、设置 |
| 主进程（Node） | 所有 fs 文件操作、路径沙箱校验、版本存储管理、窗口管理 |
| Preload | contextBridge 暴露受控 API（文件读写 / 版本查询 / 窗口操作） |

**3.1.1** 能力经 `ipcRenderer.invoke` → 主进程 `ipcMain.handle` 暴露；preload 不直接 require Node 模块（sandbox 下不可用）。

**3.1.2** IPC 通道命名遵循统一前缀规范（`fs:` / `win:` / `editor:`），并预留 `agent:` / `llm:` 前缀供未来 AI 扩展（详见 §7.4）。

### 3.2 安全设计

**3.2.1** 窗口安全基线：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`。

**3.2.2** 路径沙箱（pathGuard）：所有文件读写必须通过校验——(a) 拒绝绝对路径穿越；(b) 拒绝 `../` 相对穿越；(c) 拒绝符号链接 realpath 逃逸；最终解析路径必须落在工作区根目录子树内。

**3.2.3** 无网络请求（当前版本不含 AI，无外连需求）。

**3.2.4** 无 API Key 等敏感配置存储（当前版本）。

### 3.3 数据与存储布局

```
<工作区根目录>/                  ← 用户授权的真实磁盘文件夹（工作区，只放用户自己的文件）
├── 用户自由目录结构/             ← 用户自行组织的 MD/TXT 文件与文件夹
└── .wr/                         ← 软件衍生数据目录（隐藏，由软件统一管理）
    └── versions/<文档相对路径>/ ← 版本库（V{n}.md 递增不重排 + meta.json）
```

**3.3.1** 相比 WorkRecord，**移除**以下系统目录 / 文件（对应已砍功能）：`_folder.md`（AI 归档知识基座）、`__archive__/`（归档空间）、`__draft__/`（AI 草稿箱）、`.assets/`（图片资产，MVP 未实现项）、`.wr-plans.json`（计划 / 日程数据层）。

**3.3.2** 版本库等衍生数据**集中**存于软件专属隐藏目录（`.wr/`），保持工作区文件夹干净（见 4.3.7 / 4.5.1）；工作区文件夹整体移动时衍生目录一并迁移（可移植）。**命名定案：`.wr/`（2026-08-09，阶段 2.6）**——沿用在代码/文档中一致使用的占位名，语义清晰（work record 衍生数据），不再变更。版本库内部结构见 §4.5.1。

### 3.4 前端 UI 风格

**3.4.1** 设计基调：仿 Apple（macOS / iOS）——简洁、克制、充足留白；圆角、柔和阴影、半透明 / 毛玻璃效果（侧边栏、面板）。

**3.4.2** 配色：中性低饱和、强调色克制；遵循系统语义色。

**3.4.3** 字体：系统字体栈优先（`-apple-system`、SF Pro，跨平台备选 Inter / 思源黑体）；提供 Inter / 系统默认 / 等宽三选（CSS 变量驱动）。

**3.4.4** 图标：线性风格（类 SF Symbols），统一描边。

**3.4.5** 双模式：浅色 / 深色，手动切换 + 跟随系统（`prefers-color-scheme`）；CSS 变量 / 主题 token 统一管理，全组件自动适配；偏好持久化于 localStorage。

**3.4.6** 响应式布局：窗口缩放时界面自适应——侧边栏可折叠 / 可调宽，分屏编辑区与预览区比例随窗口合理调整，标签栏、工具栏在小窗口下不溢出、可收起。

### 3.5 模块化设计原则

**3.5.1** 高扩展性：主进程能力以模块化组织（工厂函数 + 依赖注入，便于单测与替换），模块边界清晰、职责单一；渲染侧 store / 组件分层管理。

**3.5.2** 预留冗余：为未来扩展预留接口与占位——fs 能力统一抽象（§7.1）、IPC 通道前缀预留（§3.1.2）、未来 AI 模块目录预留（§7.2）；不把模块写死、不做不可逆设计。

**3.5.3** 设计约束：新增模块优先复用既有抽象，避免重复实现；敏感操作（文件写、删除）保留二次确认 / 守卫等冗余保护。

---

## 4. 功能需求

> 编号规则：`4.<模块>.<条目>`。⚠ 标注的为开放问题，见 §8。

### 4.1 工作区设定

**4.1.1** 首次启动引导：首次运行无工作区时，弹出引导界面，让用户选择一个本地文件夹作为工作区根目录。

**4.1.2** 持久化记忆：工作区根目录路径持久化（localStorage），应用重启后自动恢复上次工作区，无需重新选择。

**4.1.3** 更换工作区：设置页提供「更换工作区」入口，可切换至其他文件夹；**切换前若有未保存标签，先提示保存 / 放弃**；切换后**清空标签页**，界面刷新为新工作区内容。

**4.1.4** 工作区有效性：启动时校验记忆的工作区路径仍存在且可访问；失效时给出提示并引导重新选择。

**4.1.5** 多工作区：支持维护多个工作区，可互相切换；提供**最近工作区列表**，在启动 / 切换入口一键快速切换。

**4.1.6** 设计说明：工作区功能将**重新优化设计**（不参考 WorkRecord 既有实现）；上述为基础需求，具体交互（数据隔离、切换形态等）在设计阶段细化（§8）。

### 4.2 MD 编辑器（三态切换）

**4.2.1** 默认分屏：主内容区默认以「编辑 | 预览」分屏并排展示；支持交互调整两者比例（拖拽分隔条），并支持隐藏其中一区（收起预览仅编辑 / 收起编辑仅预览）；工具栏提供 分屏 / 仅编辑 / 仅预览 切换。

**4.2.2** 自动保存：输入停止约 1 秒后防抖触发自动保存；持续输入不触发（防抖生效）。

**4.2.3** 手动保存：工具栏「保存」按钮 + `Ctrl+S` 快捷键即时保存。

**4.2.4** 保存状态指示：工具栏显示 已保存 / 保存中 / 未保存 状态。

**4.2.5** 内容加载：点击目录树文件 → 编辑器加载内容；加载失败（文件被删 / 无权限）给出明确错误提示。

**4.2.6** 分屏滚动：分屏模式下编辑区与预览区支持同步滚动（滚动位置联动）。

**4.2.7** 专注模式：放大主内容区，隐藏侧边栏与顶栏，最大化编辑 / 阅读空间；提供退出与临时唤出方式（快捷键 / Esc / 悬停边缘）。

### 4.3 目录树与文件管理

**4.3.1** 目录树：递归展示工作区根目录下的文件夹与 MD/TXT 文件（展开时才加载子级，懒加载），支持展开 / 折叠。

**4.3.2** 点击打开：点击文件节点 → 在编辑器标签中打开该文件。

**4.3.3** 新建：支持在工作区 / 当前文件夹下新建 MD 文件、新建文件夹。

**4.3.4** 重命名：支持文件与文件夹重命名。文件重命名同步迁移其版本库（`.wr/versions/<旧路径>` → `<新路径>`）；**文件夹重命名**时，其下所有文档的版本库按路径前缀整体递归迁移（`.wr/versions/<旧路径>/**` → `<新路径>/**`）。

**4.3.5** 删除：支持删除文件与文件夹；**删除前二次确认**；非空文件夹删除时额外警告。**删除时其版本历史（`.wr/versions/` 对应目录）一并清空**，不做恢复（最小化原则）；删除文件夹时递归清空其下所有文档的版本库。

**4.3.6** 刷新：手动刷新目录树，感知工作区外部改动（单向同步边界，见决策 2.5）；**若打开中的文件在外部被改动，保存时覆盖前提示**确认。

**4.3.7** 工作区目录整洁：保持工作区文件夹干净——用户目录中不写入任何软件衍生文件；所有衍生文件 / 文件夹（版本库等）集中存放在软件专属隐藏目录（§3.3 `.wr/`），由软件统一管理。

### 4.4 MD 预览

**4.4.1** 语法渲染：支持标准 Markdown 渲染——标题 / 列表 / 表格 / 代码块 / 引用 / 图片 / 链接 / 行内样式。

**4.4.2** 图片路径解析：相对路径图片基于当前文件所在目录解析。

**4.4.3** 外链：外部链接用系统浏览器打开（`shell.openExternal`）。

**4.4.4** 预览呈现：默认分屏态「编辑 | 预览」并排展示渲染结果（比例可调、可隐藏，同 4.2.1）；也可切至仅预览态查看。

**4.4.5** 远程图片：支持加载并显示文档引用的远程图片（`https://`）；加载远程图片属**内容呈现**，不视为违反离线原则（§5.1）。

### 4.5 版本历史

**4.5.1** 存储：版本快照集中存放于软件专属隐藏目录（§3.3，遵循 4.3.7 工作区目录整洁原则），按 `<文档相对路径>/` 组织；完整内容快照（`V1.md`、`V2.md`…递增不重排）+ `meta.json` 元数据（`versionId` / `editedBy` / `ts`；删除即整体清空、不软删除，见 4.3.5）。

**4.5.2** 触发策略：每次保存（自动或手动）产生一个新版本；若内容与上一版完全相同则不产生。回滚操作也产生一条新版本（`editedBy: rollback`）。

**4.5.3** 保留策略（确定性规则）：始终保留**最近 10 版**全量快照；对更早版本，按序号**每 10 版保留 1 版**作为里程碑快照；**每次保存**时校验——若该文档版本总数 > 50，则执行精简（删除非里程碑且不属于最近 10 版的旧快照，版本号不重排）。触发粒度：每次保存（自动 / 手动均触发；内容无变化不记版，见 4.5.2）。

**4.5.4** 版本列表：当前文件入口可打开版本列表，展示各版本时间与操作来源。

**4.5.5** 对比：支持任意两版行级 diff 对比（可视阅读）。

**4.5.6** 回滚：可回滚到任意历史版本；回滚前版本保留（回滚产生新版本，不覆盖历史）。

**4.5.7** 导出：可将某个历史版本导出为独立文件；走系统「另存为」对话框，**默认落点在工作区外**，避免被目录树 / 版本库再管理。

### 4.6 明暗主题

**4.6.1** 双模式：浅色 / 深色。

**4.6.2** 切换方式：手动切换 + 跟随系统（`prefers-color-scheme`）；手动选择后不再自动跟随，除非切回「跟随系统」。

**4.6.3** token 体系：CSS 变量 / 主题 token 统一管理，全组件自动适配，无硬编码颜色。

**4.6.4** 字体切换：Inter / 系统默认 / 等宽 三选（CSS 变量 `--font-ui` 驱动）；**汉字同样应用字体样式**——中文文本随字体选择生效；等宽模式下汉字无原生等宽，回退系统中文方案 / 统一宽度处理。

**4.6.5** 持久化：主题与字体偏好存于 localStorage，重启恢复。

### 4.7 标签页 / 多窗口

**4.7.1** 标签页：编辑器支持多文档以标签页形式同时打开；点击文件打开或激活对应标签。

**4.7.2** 标签操作：关闭标签；关闭未保存标签时提示保存 / 放弃。

**4.7.3** 标签状态持久化：记住打开的文件列表，应用重启后恢复。

**4.7.4** 多窗口（后续迭代）：支持「在新窗口打开」独立窗口，共享同一工作区；MVP 先实现标签页，多窗口列后续迭代（用户已确认）。

### 4.8 设置页

**4.8.1** 入口：顶栏齿轮图标 → 设置弹窗。

**4.8.2** 分组：外观 / 编辑器 / 工作区 / AI（预留）/ 关于。

**4.8.3** 外观组：主题切换（浅色 / 深色 / 跟随系统）、字体选择（Inter / 系统 / 等宽）。

**4.8.4** 编辑器组：自动保存开关、保存防抖间隔（默认 1s）、编辑器字号。

**4.8.5** 工作区组：显示当前工作区路径、「更换工作区」入口。

**4.8.6** AI 组（预留）：灰色不可用，标注「将在未来版本中提供」，不实现任何功能。

**4.8.7** 关于组：产品名 MeWork、版本号、来源说明。

---

## 5. 非功能需求

**5.1** 离线：核心功能全离线可用、无必需的网络依赖；加载文档中引用的远程图片（内容呈现，见 4.4.5）除外。

**5.2** 安全：路径沙箱为硬约束（3.2.2）；窗口安全基线（3.2.1）；删除等敏感操作二次确认。

**5.3** 性能：大目录树懒加载 / 展开时才加载子级；版本分级抽样控制磁盘占用；防抖保存减少 IO；应用启动快、资源占用低。

**5.4** 可靠性：文件读写、加载失败等异常有明确错误提示；重要操作留日志便于排查。

**5.5** 响应式：窗口缩放布局自适应（3.4.6）；最小可用窗口尺寸有下限，避免布局崩坏。

**5.6** 环境：单人自用；Windows 桌面（打包为可分发的安装包）。

---

## 6. 范围界定

### 6.1 MVP（首个正式版本 v1.0.0）
- 工作区设定：引导选择、持久化恢复、多工作区、最近工作区列表、更换工作区
- MD 编辑器：默认分屏「编辑 | 预览」、比例可调、可隐藏、三态切换、自动保存（1s 防抖）、Ctrl+S、保存状态指示
- 专注模式：放大主内容区、隐藏侧边栏与上方栏
- 目录树与文件管理：递归展示、点击打开、新建、重命名、删除（二次确认）、手动刷新
- MD 预览：标准语法渲染、相对图片路径、外链系统打开
- 版本历史：`.wr/versions/` 集中存储、每次保存记版、分级抽样精简、行级 diff、回滚、导出
- 标签页：多文档并开、关闭未保存提示、状态持久化恢复
- 明暗主题：浅色/深色、跟随系统、字体切换（含汉字）
- 设置页：外观 / 编辑器 / 工作区 / AI（预留）/ 关于

### 6.2 后续迭代（MVP 之后）
- 多窗口（独立窗口共享工作区）
- AI 功能：智能归档 / AI 整理 / AI 优化 / Agent 引擎 / 模型配置（架构预留，见 §7）
- 全文搜索
- 快速录入 Quick Capture
- 第三方改动自动检测（MVP 为手动刷新）
- 多设备同步

### 6.3 需求覆盖核对表（对照 WorkRecord，防遗漏）

| WorkRecord 功能模块 | 去向 | 说明 |
|---|---|---|
| 工作区设定 | ✅ 保留 | 优化重设计（多工作区、最近列表） |
| 目录树与文件夹管理 | ✅ 保留 | 新建/重命名/删除 |
| MD 编辑器（三态/自动保存/专注） | ✅ 保留 | 专注模式纳入 MVP |
| MD 预览 | ✅ 保留 | 默认分屏呈现 |
| 版本历史 / 版本对比 | ✅ 保留 | 集中存储、分级抽样、diff、回滚、导出 |
| 明暗主题 / 字体 | ✅ 保留 | 汉字随字体样式生效 |
| 设置页（外观/工作区分组） | ✅ 保留 | AI 分组预留 |
| 标签页 / 多窗口 | 🆕 新增 | 标签页进 MVP；多窗口后续迭代 |
| 智能归档 | ❌ 砍掉 → 未来迭代 | 归入 AI 功能 §7 |
| AI 整理 / AI 优化 | ❌ 砍掉 → 未来迭代 | 归入 AI 功能 §7 |
| Agent 引擎 / 编排 / 审查 | ❌ 砍掉 → 未来迭代 | 架构预留 §7 |
| 模型配置 / LLM Provider | ❌ 砍掉 → 未来迭代 | 架构预留 §7 |
| 文档模板（BUG/计划） | ❌ 砍掉 | 未列入迭代规划 |
| 计划 / 日程 / 提醒 | ❌ 砍掉 | 未列入迭代规划 |
| 全文搜索 | ⏳ 后续迭代 | 未列入 MVP |
| 快速录入 Quick Capture | ⏳ 后续迭代 | 未列入 MVP |
| 第三方改动自动检测 | ⏳ 部分保留 | MVP 手动刷新；自动检测后续迭代 |
| `_folder.md` 知识基座 / 归档空间 / 草稿箱 | ❌ 砍掉 | 对应 AI 功能移除 |
| 多设备同步 | ⏳ 后续迭代 | 未列入 MVP |

---

## 7. 架构为未来 AI 扩展预留

> 本产品定位「未来扩展项目」：当前版本不含任何 AI，但架构从第一天起预留以最小代价加回 AI 的扩展点，不阻碍当前功能。

**7.1** fs 能力抽象层：所有文件读写经统一模块（如 `electron/main/storage/fs-ops.js`）导出，不在 IPC handler 中直接调用 `fs.promises`；函数签名保持可被未来 Agent 工具层（L0 tools）直接复用。

**7.2** Provider / Agent 扩展点：预留 `electron/main/llm/`、`electron/main/agent/` 模块目录约定；未来加回时直接从 WorkRecord 搬运已验证实现并适配新 IPC 通道。

**7.3** 设置页 AI 分组占位：UI 预留 `#settings-ai` 分组（灰色不可用 + 「将在未来版本中提供」），未来直接填充。

**7.4** IPC 通道前缀预留：现行前缀 `fs:` / `win:` / `editor:`；预留 `agent:` / `llm:`，避免未来命名冲突（3.1.2）。

**7.5** 设计原则：当前不提前引入空接口（YAGNI），但保持模块边界清晰、命名规范一致、能力抽象到位，使未来扩展对现有功能零破坏。

---

## 8. 待确认 / 开放问题

### 8.1 本次逐行确认已敲定（记录在案）

| 项 | 决策 |
|---|---|
| 版本触发粒度 | 每次保存（内容无变化不记版） |
| 多窗口排期 | 后续迭代（MVP 只做标签页） |
| 多工作区 | 纳入 MVP（含最近工作区列表） |
| 专注模式 | 纳入 MVP（放大主内容区、隐藏侧边栏与上方栏） |
| 衍生数据管理 | 集中 `.wr/`，保持工作区目录干净 |
| 字体样式 | 汉字同样应用字体样式 |
| 产品名 | MeWork（2026-08-08 定名） |

### 8.2 留待设计阶段细化（非阻塞，本 PRD 不锁定）

1. 工作区功能的具体交互设计（多工作区数据隔离、切换形态）——用户要求**重新优化设计**，不参考 WorkRecord 既有实现
2. ~~`.wr/` 衍生目录的最终命名~~ **已定案（2026-08-09，阶段 2.6）：沿用 `.wr/`**，见 §3.3.2
3. 专注模式的默认快捷键与唤出交互细节
4. 响应式布局的断点与行为细则
5. 快捷键体系完整映射（除 Ctrl+S 外）
6. 编辑器技术选型与实现细节（文本编辑组件、预览渲染引擎）
7. 文件读写编码约定（UTF-8，写入不含 BOM）——影响跨编辑器可移植
8. 运行日志落点（建议 `.wr/logs/`）
9. 多工作区下 pathGuard 以当前激活工作区为界；最近工作区列表中失效路径的提示与移除

### 8.3 待用户拍板

（当前无待拍板项；后续新开放问题在此登记。）
