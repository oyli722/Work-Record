/**
 * 软件衍生数据目录名（PRD §3.3 / §3.3.2，命名定案 2026-08-09）
 * 集中存放版本库等衍生数据，保持工作区文件夹干净（§4.3.7）。
 * 全项目统一引用此常量，避免魔法字符串散落。
 * 注：目录树等 UI 的隐藏逻辑复用 fs-ops.listDirectory 的过滤（见 fs-ops.mjs），渲染进程无需镜像。
 */

export const WR_DIR_NAME = '.wr'
