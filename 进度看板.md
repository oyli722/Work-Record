# 进度看板（MeWork）

> 本文件是开发进度的**单一事实源**：记录每个阶段下的子阶段与完成状态。
> **每完成一个子阶段，立即更新本文件并提交。**
> 配套：阶段目标与验收见《开发计划.md》；完整需求见《需求规格文档.md》；AI 工作流见《CLAUDE.md》。

## 📍 当前位置

- **当前阶段**：阶段 4 · navigation（目录树与文件管理）🔄 进行中（4.1 完成，共 6 子阶段）
- **下一个待办**：4.2 新建（MD 文件 / 文件夹，悬停操作按钮 + 目录树顶部全局新建）
- **最近 tag**：`v0.2.0-editor`（阶段 4 完成时打 `v0.2.1-navigation`）

## 进度总览

| 阶段 | 代号 | 方向 | 完成 | 状态 | tag |
|---|---|---|---|---|---|
| 0 | workflow | 项目基础设施 | 8/8 | ✅ 完成 | `v0.0.1-workflow` |
| 1 | scaffold | Electron + React 空壳 | 6/6 | ✅ 完成 | `v0.0.2-scaffold` |
| 2 | workspace | 工作区与文件系统 | 7/7 | ✅ 完成 | `v0.1.0-workspace` |
| 3 | editor | MD 编辑器与预览 | 10/10 | ✅ 完成 | `v0.2.0-editor` |
| 4 | navigation | 目录树与文件管理 | 1/6 | 🔄 进行中 | — |
| 5 | version | 版本历史 | — | ⬜ 待开始 | — |
| 6 | theme | 主题系统完善 | — | ⬜ 待开始 | — |
| 7 | tabs | 标签页 | — | ⬜ 待开始 | — |
| 8 | polish | 设置页与交付 | — | ⬜ 待开始 | — |

图例：⬜ 待开始 · 🔄 进行中 · ✅ 完成

---

## 子阶段清单

### 阶段 0 · 项目基础设施（workflow）

| # | 方向 | 状态 |
|---|---|---|
| 0.1 | git init + .gitignore + .gitattributes | ✅ 完成 |
| 0.2 | 需求规格文档.md（PRD v1.1 定稿，经评审修订） | ✅ 完成 |
| 0.3 | 开发计划.md（阶段 0–8） | ✅ 完成 |
| 0.4 | 进度看板.md（本文件） | ✅ 完成 |
| 0.5 | CLAUDE.md + README.md | ✅ 完成 |
| 0.6 | docs/ 目录 + .gitkeep | ✅ 完成 |
| 0.7 | .claude 配置（settings.local.json + scheduled_tasks.json） | ✅ 完成 |
| 0.8 | 基线提交 + tag `v0.0.1-workflow` | ✅ 完成 |

### 阶段 1 · scaffold（Electron + React 可运行空壳）

| # | 方向 | 状态 |
|---|---|---|
| 1.1 | 脚手架初始化：electron-vite + `package.json` + 开发/构建/测试脚本 + 目录骨架 | ✅ 完成 |
| 1.2 | 三进程骨架 + 安全基线（main / preload / renderer，`contextIsolation:true` / `nodeIntegration:false` / `sandbox:true`） | ✅ 完成 |
| 1.3 | contextBridge 最小受控 API（`fs:ping` 连通性测试） | ✅ 完成 |
| 1.4 | 主题 token 地基（CSS 变量：颜色/间距/圆角/阴影/字体，浅色/深色两套 + 手动切换） | ✅ 完成 |
| 1.5 | 工程化：ESLint + Prettier 配置与脚本 | ✅ 完成 |
| 1.6 | 验收自测 + tag `v0.0.2-scaffold` | ✅ 完成 |

### 阶段 2 · workspace（工作区设定与文件系统数据层）

| # | 方向 | 状态 |
|---|---|---|
| 2.1 | pathGuard 路径沙箱（工厂函数：拒绝绝对路径穿越 / `../` 穿越 / symlink realpath 逃逸，含单测） | ✅ 完成 |
| 2.2 | fs-ops 受控 fs 抽象层（`storage/fs-ops.js` 工厂，read/write/list/mkdir/rename/delete 全部经 guard，含单测） | ✅ 完成 |
| 2.3 | IPC fs 受控 API（`fs:read_file`/`write_file`/`list_directory`/`mkdir`/`rename`/`delete`/`choose_directory`，preload 暴露） | ✅ 完成 |
| 2.4 | 工作区数据层（主进程 workspace 模块：激活/校验有效性；渲染进程 localStorage 最近工作区列表） | ✅ 完成 |
| 2.5 | 工作区 UI（首次引导界面 + 顶栏最近工作区切换入口 + 更换，空态/失效路径提示） | ✅ 完成 |
| 2.6 | 衍生数据目录 `.wr/` 命名定案 + 初始化（隐藏、目录树排除基础，PRD §3.3/§4.3.7） | ✅ 完成 |
| 2.7 | 验收自测 + tag `v0.1.0-workspace` | ✅ 完成 |

### 阶段 3 · editor（MD 编辑器与预览）

> 子阶段细化与选型定稿于 2026-08-09（用户确认：编辑器 **CodeMirror 6**；预览 **markdown-it + DOMPurify**；图片 **自定义协议 mework-file://**；入口 **侧边栏极简文件列表**）。

| # | 方向 | 状态 |
|---|---|---|
| 3.1 | 编辑器数据层：editorStore（打开/内容/脏标记/保存状态）+ 经 fs-ops 真实落盘 + 加载失败明确报错（PRD §4.2.5）；**侧边栏目录树最小形态**（**极简文件夹图标置树区顶部、默认收起、点击一键展开/收回**；展示嵌套子文件夹、**点击整行展开/关闭、懒加载子级**、MD/TXT 文件点击打开；阶段 4 演进完整目录树 CRUD） | ✅ 完成 |
| 3.2 | 三态编辑器布局：**CodeMirror 6** 编辑区 + 默认「编辑｜预览」分屏、拖拽调比例、可隐藏一侧、工具栏 分屏/仅编辑/仅预览（PRD §4.2.1 / §4.4.4） | ✅ 完成 |
| 3.3 | 保存体系：自动保存（停止 ~**30s** 防抖，v1.3 定案）+ 手动保存（按钮 + Ctrl+S）+ 保存状态指示 已保存/保存中/未保存 + 内容无变化不写盘（PRD §4.2.2–4.2.4） | ✅ 完成 |
| 3.4 | MD 预览渲染：**markdown-it + DOMPurify** 渲染标准语法（标题/列表/表格/代码块/引用/图片/链接/行内样式）+ 暖纸阅读面样式落地（PRD §4.4.1） | ✅ 完成 |
| 3.5 | 预览图片：**自定义协议 mework-file://**（主进程经 pathGuard 校验返回文件流）+ 远程 https 图片直接加载（PRD §4.4.2 / §4.4.5） | ✅ 完成 |
| 3.6 | 外链：预览链接用系统浏览器打开（PRD §4.4.3） | ✅ 完成 |
| 3.7 | 分屏同步滚动：CodeMirror 精确滚动事件 + 预览区滚动联动（PRD §4.2.6） | ✅ 完成 |
| 3.8 | 专注模式：隐藏侧边栏与顶栏、最大化编辑区；Esc/快捷键/悬停边缘唤出（PRD §4.2.7） | ✅ 完成 |
| 3.9 | 编辑器设置数据层：自动保存开关、防抖间隔、字号（localStorage；UI 阶段 8 呈现） | ✅ 完成 |
| 3.10 | 验收自测 + tag `v0.2.0-editor` | ✅ 完成 |

### 阶段 4 · navigation（目录树与文件管理）

> 子阶段规划定稿于 2026-08-09（用户确认：CRUD 交互=悬停操作按钮；版本库迁移=阶段 4 预留调用；删除确认=自定义弹窗）。

| # | 方向 | 状态 |
|---|---|---|
| 4.1 | 目录树数据层重构：`fs:list_detail` 聚合接口（list+stat 一次返回，解决 IPC 洪泛 S1）+ 渲染侧不可变 reducer（S2） | ✅ 完成 |
| 4.2 | 新建：MD 文件 / 文件夹（PRD §4.3.3） | ⬜ |
| 4.3 | 重命名：文件/文件夹 + 版本库路径前缀递归迁移（§4.3.4） | ⬜ |
| 4.4 | 删除：二次确认（自定义弹窗）+ 非空文件夹警告 + 版本库清空（§4.3.5） | ⬜ |
| 4.5 | 刷新感知外部改动 + 打开中文件外部改动保存前提示（§4.3.6） | ⬜ |
| 4.6 | 验收自测 + tag `v0.2.1-navigation` | ⬜ |

---

## 待办区

（开发中发现需求-实现 gap 或候选优化，登记于此。）

- **[已定] 目标平台扩展 Windows/Mac（2026-08-09）**：PRD §1.3.2 形态由「Windows」扩展为「Windows/Mac」。影响：阶段 8 打包安装需兼顾 macOS；路径/平台相关实现（pathGuard、mework-file 等）保持平台无关写法；macOS 惯例（Dock 点击重建窗口）主进程已预留。开发阶段仍以 Windows 为主验证。
- **[已定] 样式确认（2026-08-09，阶段 2 前）**：沿用 Apple 克制风（PRD §3.4，tokens.css 现状）+ 采纳「内容区暖纸色阅读面」`--color-paper`（浅 `#faf9f7` / 深 `#21201e`），与冷色玻璃外壳对比；阶段 3 编辑器/预览落地。
- **[已定] 工作区布局定案（2026-08-09，阶段 2 收尾）**：顶栏极简（标题 + 主题/设置全局操作）；**工作区路径与切换归入目录树侧边栏顶部**；主区 = 内容工具条 + 编辑器/预览（阶段3）+ 标签栏（阶段7）。布局面向完整形态，避免阶段 3/4/7 返工结构。
- **[已定] 删除工作区语义（2026-08-09）**：仅提供「从最近列表移除」（移除记录，不碰磁盘任何文件，侧边栏菜单 + 引导页均有入口）；**不提供物理删除工作区目录**（工作区=用户真实文件夹，软件不代删，与 PRD §1.2.1 定位一致）。PRD §8.3 待拍板区已定。
- **[已定] 侧边栏拖拽调宽 / 折叠（2026-08-09）**：按开发计划**阶段 8「响应式布局完善」**实现（PRD §3.4.6：侧边栏可折叠/可调宽）。阶段 3 仅做主区「编辑｜预览」分屏比例拖拽（3.2，PRD §4.2.1），两者交互相近但归属不同。
- **[已定] 自动保存间隔 30s（2026-08-09，阶段 3.3）**：自动保存防抖由 PRD §4.2.2 原 1s 改为**停止编辑约 30s** 触发（用户确认）。PRD 升 **v1.3**。3.9 编辑器设置数据层以此默认可配置。
- **[已完成] 阶段 3.1 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md` 逐条处理）**：
  - **P1** doSave 保存期间编辑竞态：落盘后比对 `contentRef` 与快照，期间有新编辑保持 dirty（关闭 G1）
  - **P2** 切换工作区目录树刷新：成功激活后清空并收起目录树，重新展开加载新工作区（关闭 G2）
  - **P3** 切换前未保存内容保存失败则中止切换、保留现场（PRD §4.1.6，关闭 G3）
  - **S3** 无工作区时目录树 toggle 禁用；**S4** 编辑区打开后自动聚焦
- **[已登记] 阶段 3.1 评审 S 类（留待阶段 4/8）**：S1 `buildNodes` 逐目录项 `stat`（IPC 洪泛，大目录卡顿）→ 阶段 4/8 提供「list + stat」聚合返回；S2 `toggleFolder` 直接 mutate 节点对象 → 阶段 4 演进 tree reducer / 不可变更新。
- **[已完成] 阶段 3.2/3.3 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md`）**：无 P 类；S-3.3-1 已修复（doSave 保存失败统一 setError，自动保存失败也提示原因）；观察项 O-3.3-1/O-3.3-2 接受。
- **[已登记] S-3.2-1（留待阶段 7）**：CodeMirror 外部 value 同步 dispatch 触发一次多余 `onChange` → 阶段 7 多标签页优化（代价低）。
- **[候选] 图片粘贴资产管理（2026-08-09，未排期）**：编辑器粘贴图片（剪贴板图片 / 本地文件路径）→ 自动保存到工作区并生成 markdown 图片语法 `![](相对路径)`。**需拍板落盘位置**（用户可见目录如 `images/` vs 衍生目录 `.wr/`；工作区外绝对路径仍被 pathGuard 拒绝）。
- **[已完成] 阶段 3.4/3.5 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md`）**：
  - **P1（安全必修复）** DOMPurify URI 白名单正则改回官方语义（第三分支保留冒号排除）+ 追加 `mework-file`——先前改写致 `javascript:`/`vbscript:`/`file:` 放行（XSS 漏洞）。实测拒绝 + markdown-it `validateLink` 双保险，本地图正常
  - **S1** `decodeURIComponent` 畸形 URL（非法 `%`）包 try 返回 400
  - **S2** 协议响应 `ACAO: *` 评估接受（无凭据 + 仅应用内 + file:// 跨源 img 需要，收窄会破坏生产；登记观察）
- **[已定] 外链协议白名单（2026-08-09，阶段 3.6）**：预览文本链接仅 http/https/mailto/tel 交系统打开（`shell.openExternal`，PRD §4.4.3 范围细化）；**图片不受影响仍在应用内显示**（3.5）；白名单单一来源 `src/shared/link-policy.js`（评审 S3）。
- **[已完成] 阶段 3.6 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md` 逐条处理，无 P 类）**：
  - **S1** `will-navigate` 前缀匹配改 origin 精确比较（防 `localhost:5173.evil.com` 绕过）
  - **S2** `win:open_external` 改 async + await，打开失败返回 `{ok:false, reason}`
  - **S3** 外链白名单抽 `src/shared/link-policy.js` 单一来源（主进程/渲染层同引用）
  - **S4** 正则收紧 `^(https?:\/\/|mailto|tel):`（拒 `http:foo` 畸形）
  - O1/O3 接受现状（锚点与打开失败静默）；O2 白名单决策已登本区
- **[已完成] 阶段 3.7 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md` 逐条处理）**：
  - **P1** 底部边界：预览 `scrollToLine`/`computeTopLine` 末尾无锚点回退最后锚点（滚到底两侧不脱节）
  - **S1** 驱动守卫方向互换（编辑 handler 守卫 `preview`、预览 handler 守卫 `editor`，消除潜在回环）
  - **S2** 锚点缓存 `{srcLine, top, bottom}` 免 reflow（滚动二分直比 `scrollTop`；`scrollToLine` 改二分）
  - O1/O2/O3 接受（块起始行粒度 / th/td 无锚点 / 同源行重复，文档序二分正确）
- **[已完成] 阶段 3.8 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md` 逐条处理）**：
  - **P1** 目录树状态提升到 App（tree/listOpen/listError/listLoading 共享，进出专注不丢失）
  - **S1** `useHover` 卸载清理定时器
  - **S2** 保存状态随工具条隐藏接受现状（「工具条也隐藏」为用户定案，Ctrl+S 仍可用）
  - **S3** 未激活工作区 F11 放行不吞
  - O1/O2/O3 接受（Esc 冒泡 / focus 中切换失败边缘 / 热区遮边缘）
- **[已完成] 阶段 3.9 评审闭环（2026-08-09，`docs/阶段3-editor-评审记录.md` 逐条处理，无 P 类）**：
  - **S1** 字号 CSS 变量 fallback 已带（核实）
  - **S2** `autosaveDelayMs` clamp [1s, 10min]
  - **S3** sanitize 排除数组
  - **O1** `useEditor` 只传自动保存所需参数
  - O2 设置 setter 就绪，阶段 8 设置页接 UI
- **[已完成] 阶段 2 评审闭环（2026-08-09，`docs/阶段2-workspace-评审记录.md` 逐条处理）**：
  - **P1** 激活失败状态机：失败回引导态 + 错误文案统一展示（store/空状态/切换路径全覆盖）+ 清空失效记忆（PRD §4.1.4/§4.1.5，关闭 G1）
  - **S1** `.wr/` 从 `fs-ops.listDirectory` 排除 + 单测（关闭 G2）
  - **S2** 工作区写权限以 `.wr/` 初始化实际可写性兜底报错（Windows 目录 access W_OK 不可靠）
  - **S3** `index.js` 顶部注释更新至阶段 2 现状（fs-ops.mjs + pathGuard）
  - **S4** 首帧恢复提示：`activating` 态主区显示「正在恢复工作区…」，消除「选择工作区」闪烁

---

## 更新规则

- **开始某子阶段**：状态改为 🔄。
- **完成某子阶段**：勾选 ✅，更新「📍 当前位置」与「进度总览」计数，**当次提交**（Conventional Commits，信息含子阶段号）。
- **阶段全部完成**：依《开发计划.md》验收标准自测 → 打阶段 tag → 总览状态改 ✅。
- **需求-实现 gap**：核对需求覆盖发现 → 排期归位进子阶段清单；未排期暂列「待办区」。
