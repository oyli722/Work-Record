# 进度看板 V1 · 开发日志归档（阶段 0–9）

> 本文件为《进度看板.md》V1 的历史开发日志归档（2026-08-12，阶段 0–9 全部完成后归档）。
> 记录阶段 0–9 全部子阶段的开发与完成状态；**当前进度以《进度看板.md》为准**，本文件仅作历史留档。
> 配套：《需求规格文档》·《开发计划》·《进度看板》·《CLAUDE.md》

---

## 📍 归档时状态

- **阶段状态**：阶段 9 · optimize ✅ 完成（16/16 子阶段闭环 + 2 项用户定案取消：9.2.4 失焦保存 / 9.3.6 性能专项；验收自测 lint/单测/build 通过，打包用户定案跳过）
- **本轮用户反馈 6 项全部落地**：9.1.1 图标统一 / 9.2.1 工作区右键 / 9.2.2 标签批量关闭 / 9.2.3 树悬停箭头 / 9.3.1 标签多开 / 9.3.2 树层级显示
- **最近 tag**：`v1.1.0-optimize`

## 进度总览

| 阶段 | 代号 | 方向 | 完成 | 状态 | tag |
|---|---|---|---|---|---|
| 0 | workflow | 项目基础设施 | 8/8 | ✅ 完成 | `v0.0.1-workflow` |
| 1 | scaffold | Electron + React 空壳 | 6/6 | ✅ 完成 | `v0.0.2-scaffold` |
| 2 | workspace | 工作区与文件系统 | 7/7 | ✅ 完成 | `v0.1.0-workspace` |
| 3 | editor | MD 编辑器与预览 | 10/10 | ✅ 完成 | `v0.2.0-editor` |
| 4 | navigation | 目录树与文件管理 | 6/6 | ✅ 完成 | `v0.2.1-navigation` |
| 5 | version | 版本历史 | 7/7 | ✅ 完成 | `v0.3.0-version` |
| 6 | theme | 主题系统完善 | 5/5 | ✅ 完成 | `v0.4.0-theme` |
| 7 | tabs | 标签页 | 5/5 | ✅ 完成 | `v0.5.0-tabs` |
| 8 | polish | 设置页、打磨与打包交付 | 6/6 | ✅ 完成 | `v1.0.0-mvp` |
| 9 | optimize | UI/交互/功能优化与缺陷修复 | 16/16 | ✅ 完成 | `v1.1.0-optimize` |

图例：⬜ 待开始 · 🔄 进行中 · ✅ 完成


---

## 子阶段清单

### 阶段 0 · 项目基础设施（workflow）

| # | 方向 | 状态 |
|---|---|---|
| 0.1 | git init + .gitignore + .gitattributes | ✅ 完成 |
| 0.2 | 需求规格文档.md（PRD v1.1 定稿，经评审修订） | ✅ 完成 |
| 0.3 | 开发计划.md（阶段 0–8） | ✅ 完成 |
| 0.4 | 进度看板.md（本文件） | ✅ 完成 |
| 0.5 | CLAUDE.md + README.md | ✅ 完成 |
| 0.6 | docs/ 目录 + .gitkeep | ✅ 完成 |
| 0.7 | .claude 配置（settings.local.json + scheduled_tasks.json） | ✅ 完成 |
| 0.8 | 基线提交 + tag `v0.0.1-workflow` | ✅ 完成 |

### 阶段 1 · scaffold（Electron + React 可运行空壳）

| # | 方向 | 状态 |
|---|---|---|
| 1.1 | 脚手架初始化：electron-vite + `package.json` + 开发/构建/测试脚本 + 目录骨架 | ✅ 完成 |
| 1.2 | 三进程骨架 + 安全基线（main / preload / renderer，`contextIsolation:true` / `nodeIntegration:false` / `sandbox:true`） | ✅ 完成 |
| 1.3 | contextBridge 最小受控 API（`fs:ping` 连通性测试） | ✅ 完成 |
| 1.4 | 主题 token 地基（CSS 变量：颜色/间距/圆角/阴影/字体，浅色/深色两套 + 手动切换） | ✅ 完成 |
| 1.5 | 工程化：ESLint + Prettier 配置与脚本 | ✅ 完成 |
| 1.6 | 验收自测 + tag `v0.0.2-scaffold` | ✅ 完成 |

### 阶段 2 · workspace（工作区设定与文件系统数据层）

| # | 方向 | 状态 |
|---|---|---|
| 2.1 | pathGuard 路径沙箱（工厂函数：拒绝绝对路径穿越 / `../` 穿越 / symlink realpath 逃逸，含单测） | ✅ 完成 |
| 2.2 | fs-ops 受控 fs 抽象层（`storage/fs-ops.js` 工厂，read/write/list/mkdir/rename/delete 全部经 guard，含单测） | ✅ 完成 |
| 2.3 | IPC fs 受控 API（`fs:read_file`/`write_file`/`list_directory`/`mkdir`/`rename`/`delete`/`choose_directory`，preload 暴露） | ✅ 完成 |
| 2.4 | 工作区数据层（主进程 workspace 模块：激活/校验有效性；渲染进程 localStorage 最近工作区列表） | ✅ 完成 |
| 2.5 | 工作区 UI（首次引导界面 + 顶栏最近工作区切换入口 + 更换，空态/失效路径提示） | ✅ 完成 |
| 2.6 | 衍生数据目录 `.wr/` 命名定案 + 初始化（隐藏、目录树排除基础，PRD §3.3/§4.3.7） | ✅ 完成 |
| 2.7 | 验收自测 + tag `v0.1.0-workspace` | ✅ 完成 |

### 阶段 3 · editor（MD 编辑器与预览）

> 子阶段细化与选型定稿于 2026-08-09（用户确认：编辑器 **CodeMirror 6**；预览 **markdown-it + DOMPurify**；图片 **自定义协议 mework-file://**；入口 **侧边栏极简文件列表**）。

| # | 方向 | 状态 |
|---|---|---|
| 3.1 | 编辑器数据层：editorStore（打开/内容/脏标记/保存状态）+ 经 fs-ops 真实落盘 + 加载失败明确报错（PRD §4.2.5）；**侧边栏目录树最小形态**（**极简文件夹图标置树区顶部、默认收起、点击一键展开/收回**；展示嵌套子文件夹、**点击整行展开/关闭、懒加载子级**、MD/TXT 文件点击打开；阶段 4 演进完整目录树 CRUD） | ✅ 完成 |
| 3.2 | 三态编辑器布局：**CodeMirror 6** 编辑区 + 默认「编辑｜预览」分屏、拖拽调比例、可隐藏一侧、工具栏 分屏/仅编辑/仅预览（PRD §4.2.1 / §4.4.4） | ✅ 完成 |
| 3.3 | 保存体系：自动保存（停止 ~**30s** 防抖，v1.3 定案）+ 手动保存（按钮 + Ctrl+S）+ 保存状态指示 已保存/保存中/未保存 + 内容无变化不写盘（PRD §4.2.2–4.2.4） | ✅ 完成 |
| 3.4 | MD 预览渲染：**markdown-it + DOMPurify** 渲染标准语法（标题/列表/表格/代码块/引用/图片/链接/行内样式）+ 暖纸阅读面样式落地（PRD §4.4.1） | ✅ 完成 |
| 3.5 | 预览图片：**自定义协议 mework-file://**（主进程经 pathGuard 校验返回文件流）+ 远程 https 图片直接加载（PRD §4.4.2 / §4.4.5） | ✅ 完成 |
| 3.6 | 外链：预览链接用系统浏览器打开（PRD §4.4.3） | ✅ 完成 |
| 3.7 | 分屏同步滚动：CodeMirror 精确滚动事件 + 预览区滚动联动（PRD §4.2.6） | ✅ 完成 |
| 3.8 | 专注模式：隐藏侧边栏与顶栏、最大化编辑区；Esc/快捷键/悬停边缘唤出（PRD §4.2.7） | ✅ 完成 |
| 3.9 | 编辑器设置数据层：自动保存开关、防抖间隔、字号（localStorage；UI 阶段 8 呈现） | ✅ 完成 |
| 3.10 | 验收自测 + tag `v0.2.0-editor` | ✅ 完成 |

### 阶段 4 · navigation（目录树与文件管理）

> 子阶段规划定稿于 2026-08-09（用户确认：CRUD 交互=悬停操作按钮；版本库迁移=阶段 4 预留调用；删除确认=自定义弹窗）。

| # | 方向 | 状态 |
|---|---|---|
| 4.1 | 目录树数据层重构：`fs:list_detail` 聚合接口（list+stat 一次返回，解决 IPC 洪泛 S1）+ 渲染侧不可变 reducer（S2） | ✅ 完成 |
| 4.2 | 新建：MD 文件 / 文件夹（PRD §4.3.3） | ✅ 完成 |
| 4.3 | 重命名：文件/文件夹 + 版本库路径前缀递归迁移（§4.3.4） | ✅ 完成 |
| 4.4 | 删除：二次确认（自定义弹窗）+ 非空文件夹警告 + 版本库清空（§4.3.5） | ✅ 完成 |
| 4.5 | 刷新感知外部改动 + 打开中文件外部改动保存前提示（§4.3.6） | ✅ 完成 |
| 4.6 | 验收自测 + tag `v0.2.1-navigation` | ✅ 完成 |

### 阶段 5 · version（版本历史）

> 设计文档定稿于 2026-08-09（`docs/阶段5-version-设计文档.md`，含外部评审 P1-P3 / S1-S4 / O1-O4 处理）。

| # | 方向 | 状态 |
|---|---|---|
| 5.1 | 主进程 version-manager（快照 V{n}.md + meta.json + 保留策略精简，幂等）+ IPC + preload + 单测 | ✅ 完成 |
| 5.2 | 保存触发记版（editorStore `save(editedBy)`，无变化不记） | ✅ 完成 |
| 5.3 | 版本列表 UI（右键入口 + 右侧滑出面板） | ✅ 完成 |
| 5.4 | 主区对比视图（DiffView，单选对当前/双选对版本，行级 LCS 限幅） | ✅ 完成 |
| 5.5 | 回滚（独立流程：确认 → 强制落盘 + 记 rollback 版） | ✅ 完成 |
| 5.6 | 导出（另存为，默认 Documents） | ✅ 完成 |
| 5.7 | 验收自测 + tag `v0.3.0-version` | ✅ 完成 |

### 阶段 6 · theme（主题系统完善）

> 子阶段规划定稿于 2026-08-09（用户确认：主题按钮=眼睛图标三态循环——浅色睁眼/深色闭眼/跟随系统；字体三选仅 UI 字体）。

| # | 方向 | 状态 |
|---|---|---|
| 6.1 | 跟随系统主题：useTheme 三态（浅/深/跟随系统）+ `prefers-color-scheme` 监听 + 手动覆盖优先级 + 持久化 + 顶栏眼睛图标 | ✅ 完成 |
| 6.2 | 字体切换数据层：Inter / 系统默认 / 等宽 三选 + localStorage + `--font-ui` 驱动 | ✅ 完成 |
| 6.3 | 字体应用到全局 + 汉字处理（等宽模式汉字回退系统中文方案） | ✅ 完成 |
| 6.4 | token 覆盖率检查 + 硬编码颜色清理（§4.6.3） | ✅ 完成 |
| 6.5 | 验收自测 + tag `v0.4.0-theme` | ✅ 完成 |

### 阶段 7 · tabs（标签页）

> 子阶段规划定稿于 2026-08-09（用户确认：标签栏=编辑器上方一行；关闭未保存=保存/放弃/取消三选 + 检测外部改动；编辑状态=保留光标不保撤销，处理 S-3.2-1）。

| # | 方向 | 状态 |
|---|---|---|
| 7.1 | editorStore 多标签重构（单文档 → `tabs[]` + 活动标签，每标签独立内容/脏/保存/磁盘快照/自动保存计时 + S-3.2-1 抑制多余 onChange） | ✅ 完成 |
| 7.2 | 标签栏 UI（编辑器上方一行：文件名/●未保存/✕/活动高亮）+ 点击切换/新开激活 | ✅ 完成 |
| 7.3 | 关闭标签流程（三选 保存/放弃/取消 + 外部改动检测联动） | ✅ 完成 |
| 7.4 | 标签状态持久化（localStorage 打开列表，重启恢复） | ✅ 完成 |
| 7.5 | 验收自测 + tag `v0.5.0-tabs` | ✅ 完成 |

### 阶段 8 · polish（设置页、打磨与打包交付）

> 子阶段规划定稿于 2026-08-09（用户确认：设置页=左侧分组导航弹窗；侧边栏=拖拽调宽+保留收起；打包=仅 Windows NSIS）。

| # | 方向 | 状态 |
|---|---|---|
| 8.1 | 设置页 UI（弹窗 + 左侧分组：外观/编辑器/工作区/AI 预留/关于；落地 3.9 编辑器设置、6.x 主题/字体 UI） | ✅ 完成 |
| 8.2 | 异常处理与日志（主进程错误日志落 `.wr/logs/` + 空态/错误态/加载态覆盖） | ✅ 完成 |
| 8.3 | 响应式布局（侧边栏拖拽调宽 + 最小尺寸下限） | ✅ 完成 |
| 8.4 | 性能优化（大目录懒加载、外部改动检测 mtime 快照、防抖保存） | ✅ 完成 |
| 8.5 | 文件编码约定（UTF-8 无 BOM）+ 应用图标 + Windows NSIS 打包配置 | ✅ 完成 |
| 8.6 | 验收自测（单测 30/30、lint/build 通过、打包产物运行中）+ README/用户说明更新 + tag `v1.0.0-mvp` | ✅ 完成 |

### 阶段 9 · optimize（UI 视觉 / 交互逻辑 / 功能增强与性能 / 缺陷修复）

> 规划定稿于 2026-08-11（用户确认：按优化方向 4 组；缺陷「遇缺陷即修，不建清单」；**不引入颠覆性新功能**——多窗口 / AI / 全文搜索等仍归《开发计划》§四后续迭代）。
> 2026-08-11 依用户反馈细化 9.1–9.3 子阶段（图标统一 / 工作区右键 / 标签批量关闭 / 树悬停箭头 / 标签多开 / 树层级显示）。
> 本阶段以打磨 MVP 为唯一目标：优化现有功能体验、不新增需求；开发中发现缺陷随手修复并在待办区登记。

**9.1 UI 视觉优化**

| # | 方向 | 状态 |
|---|---|---|
| 9.1.1 | 图标体系统一：顶栏设置 / 编辑器专注 / 标签关闭等文本字符图标 SVG 化，与主题图标同描边同尺寸（用户反馈） | ✅ 完成 |
| 9.1.2 | token 一致性复查：间距 / 圆角 / 阴影 / 层级 token 落全组件（工具栏、标签栏、版本面板、设置页） | ✅ 完成 |
| 9.1.3 | diff 视图视觉收敛（阶段 6.4 登记 O2：配色 / 样式微调） | ✅ 完成 |
| 9.1.4 | 空态 / 错误态 / 加载态视觉统一（8.2 基础上打磨） | ✅ 完成 |

**9.2 交互逻辑优化**

| # | 方向 | 状态 |
|---|---|---|
| 9.2.1 | 工作区右键菜单：右键侧边栏工作区空白区弹出「最近使用 / 更换工作区」切换菜单（与按钮点击同菜单，用户反馈；含外部点击/Esc 关闭） | ✅ 完成 |
| 9.2.2 | 标签批量关闭：标签右键菜单 关闭 / 关闭其他 / 关闭左侧 / 关闭右侧 / 全部关闭；未保存标签逐个三选（用户定案） | ✅ 完成 |
| 9.2.3 | 文件树悬停箭头 + 点击递归展开：hover 显示 ▸/▾；点击箭头递归展开 / 收起整个子文件夹（用户定案） | ✅ 完成 |
| 9.2.4 | 保存时机优化（阶段 8.4 登记 S3：失焦 onBlur 立即保存，不必等 30s 防抖） | 🚫 已取消（2026-08-12 用户定案：不需要此功能） |
| 9.2.5 | 反馈与焦点细节（右键菜单位置避让、确认弹窗焦点管理与键盘可达） | ✅ 完成 |
| 9.2.6 | 快捷键体系补充（PRD §8.2 开放项 5：新建 / 重命名 / 删除 / 切换标签 / 切换分屏等） | ✅ 完成 |
| 9.2.7 | 编辑联动与光标定位（2026-08-12 用户定案，源自优化文档 缺陷 1）：编辑区光标行对齐预览（修文件尾部编辑预览不同步）；分屏/编辑/预览切换保留光标位置 | ✅ 完成 |
| 9.2.8 | 新建文件流程重构（2026-08-12 用户定案，源自优化文档 优化 3）：右键「新建 MD 文件」→「新建文件」先建默认名再改；右键菜单支持「在资源管理器中打开」；目录树范围扩展为 无后缀/md/txt | ✅ 完成 |

**9.3 已有功能增强与显示优化**

| # | 方向 | 状态 |
|---|---|---|
| 9.3.1 | 标签栏多开优化：活动标签自动滚动到可视区 + 鼠标滚轮横向滚动（用户确认） | ✅ 完成 |
| 9.3.2 | 文件树层级显示：层数过多时缩进封顶（第 5 层）+ 每层细缩进线（用户定案） | ✅ 完成 |
| 9.3.3 | 图片粘贴资产管理（待办区候选：粘贴图片存工作区 + 生成相对路径 markdown；需拍板落盘位置） | ✅ 完成 |
| 9.3.4 | 日志轮转（阶段 8.4 登记 S2：`.wr/logs/` 大小 / 数量上限轮转） | ✅ 完成 |
| 9.3.5 | 关于页版本号动态化（阶段 8.4 登记 O1：版本号动态读取而非硬编码） | ✅ 完成 |
| 9.3.6 | 性能优化专项（大文件编辑、大目录虚拟滚动、diff 大数据量，按需） | 🚫 已取消（2026-08-12 用户定案：现状可接受，无需专项） |

**9.4 缺陷修复**

- 规则（用户定案「遇缺陷即修，不建清单」）：开发中发现的缺陷**随所在优化子阶段顺手修复**，修复后在待办区登记；涉及面较大需专项处理的，在此建子阶段。

