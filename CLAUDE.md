# CLAUDE.md — AI 助手工作流

> 本文件是给 AI 助手（Claude）的项目操作手册。**新会话或上下文丢失后，从这里恢复。**
> 配套：《进度看板.md》（单一事实源）、《需求规格文档.md》（PRD v1.1）、《开发计划.md》（阶段里程碑）。

## 项目一句话

**MeWork** —— 本地工作记录管理桌面应用（Electron + React + JavaScript）。《WorkRecord》的极简重构：砍掉 AI 与日程，保留最核心的工作区 + 文件编辑，新增标签页；单人本地自用。**未来预留 AI 扩展**。

---

## 恢复上下文（每次开工先做）

按序读取，恢复"我们在哪、要做什么"：
1. **本文件** — 工作流与约定
2. **进度看板.md** — 当前进度与「下一个待办」（**单一事实源**）
3. **需求规格文档.md** — 完整需求与已锁定架构（实现前查相关模块）
4. **开发计划.md** — 各阶段目标与验收标准

再跑 `git log --oneline -5` 与 `git tag` 看历史进度。

## 工作循环（每次推进一个子阶段）

1. 从《进度看板.md》取「下一个待办」
2. 读《需求规格文档》对应模块 + 《开发计划》该阶段「验收标准」
3. **结合需求向用户确认**：先确认需求理解与范围，用户确认后才开始
4. 实现（遵守下方约定）
5. **任务与实际比对**：完成后将实现结果与需求 / 任务逐条比对，偏差主动登记或修正，不自认完成
6. **立即更新《进度看板.md》**：勾选完成项、更新「下一个待办」与计数
7. 提交：Conventional Commits，信息含子阶段号
8. 该阶段所有子阶段完成 → 依「验收标准」自测 → 打阶段 tag → 更新《进度看板.md》

## 不可违反的约定

- **技术栈**：Electron + React + JS；构建工具 **electron-vite**
- **进程模型**：fs 操作在**主进程**；UI 在**渲染进程**；`contextIsolation:true`、`nodeIntegration:false`、`sandbox:true`；能力经 `contextBridge` 暴露（preload 不直接 require Node 模块）
- **路径沙箱**：所有文件读写必须经过 pathGuard（限定工作区根目录内；拒绝绝对路径穿越、`../` 穿越、symlink realpath 逃逸）
- **衍生数据集中**：所有软件衍生文件 / 文件夹集中存放在工作区 `.wr/`，保持用户目录干净（PRD §3.3 / §4.3.7）
- **架构已锁定**（详见《需求规格文档》，不得擅自偏离）：
  - 当前版本**不含 AI**（不引入任何 LLM 依赖）
  - 架构预留在 `electron/main/llm/` 与 `electron/main/agent/`（未来加回 AI 的扩展点，PRD §7）
  - 所有 fs 操作统一经 `storage/fs-ops.js` 导出（为未来 Agent 工具层预留统一入口）
  - IPC 通道前缀规范：`fs:` / `win:` / `editor:`，预留 `agent:` / `llm:`
  - 模块设计**高扩展性、预留冗余**（工厂函数 + 依赖注入，PRD §3.5）
- **变更架构决策**须先问用户、升版本号并记入《需求规格文档》，不可自行其是
- **提交**：Conventional Commits（`feat / fix / docs / refactor / chore / test / style`）；每阶段一个 tag
- **沟通语言**：中文；代码注释跟随周围风格

## 验收 / 评审循环（协作方式）

- **开发模式**：AI 实现子阶段提交后，由**用户 / 外部提供验收或评审文档**（放 `docs/`，命名含对象名或子阶段号，如 `需求规格-评审记录.md`）；AI 读取后**逐条对照处理**（P 类必修复 / S 类建议），修订后闭环
- **评审记录是临时文件**：验收通过后 `rm` 删除、**不提交**（`.gitignore` 已排除 `docs/*-评审记录.md`）
- **需求-实现 gap**：发现主动登记到《进度看板.md》待办区或排期
- 当前阶段（阶段 0 · 文档搭建）：逐份文档与用户确认后定稿

## 防上下文丢失 · 硬规则

- 《进度看板.md》是当前状态的**唯一事实源**：完成即更新、即提交，不要只记在脑 / 对话里
- 一次只推进一个子阶段，做完即提交，保持工作区 `git status` 干净
- 收尾前自检：看板已更新、已提交、工作区干净
- 重要决策写入文档（需求 / 计划），不要只留在对话

## 开发命令（阶段 1 起启用）

`npm install` · `npm run dev`（开发） · `npm run build`（构建） · `npm run lint` · `npm run format`
- **单测**：`npm test` = `node --test`（阶段 1 建立后启用）

## 当前进度快照

- 当前阶段：阶段 9 · optimize 🔄（UI 视觉 / 交互逻辑 / 功能增强与性能 / 缺陷修复，规划定稿待开工）
- 下一个待办：9.1.1 视觉一致性复查（详见《进度看板.md》）
- 最近 tag：`v1.0.0-mvp`（阶段 9 完成时打 `v1.1.0-optimize`）
