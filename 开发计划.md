# 开发计划（MeWork）

> 本文件是项目开发路线图，按**阶段里程碑**组织，**不含时间表**。
> 后续开发严格依据本计划推进；每阶段验收通过对应一个 git 标签。
> 配套基准文档：《需求规格文档.md》（PRD v1.3，2026-08-09 定稿）。

## 一、git 版本管理约定

- **主分支** `main`：始终保持可运行的稳定态
- **阶段分支** `phase-N-<代号>`：较大阶段在独立分支开发，验收后合并回 `main`（单人开发也可直接提交 `main`）
- **提交信息** 遵循 Conventional Commits：`feat:` 新功能 · `fix:` 修复 · `docs:` 文档 · `refactor:` 重构 · `chore:` 构建/杂项 · `test:` 测试 · `style:` 格式
- **里程碑标签**：每阶段验收打 tag，命名见阶段总览
- **文档变更**：需求 / 设计文档变更升版本号并在「修改记录」登记
- **基线**：`v0.0.1-workflow` = 需求确认与工作流体系初始化（本次提交，即阶段 0 交付 tag）

## 二、阶段总览

| 阶段 | 代号 | 主题 | 依赖 | 标签 |
|---|---|---|---|---|
| 0 | workflow | 项目基础设施（git + 文档体系 + 工作流配置） | — | `v0.0.1-workflow` |
| 1 | scaffold | 可运行空壳（Electron + React + 主题 token 地基） | 0 | `v0.0.2-scaffold` |
| 2 | workspace | 工作区设定与文件系统数据层 | 1 | `v0.1.0-workspace` |
| 3 | editor | MD 编辑器（三态 + 自动保存 + 预览 + 专注） | 2 | `v0.2.0-editor` |
| 4 | navigation | 目录树与文件管理 | 3 | `v0.2.1-navigation` |
| 5 | version | 版本历史（快照 + 确定性保留 + diff + 回滚 + 导出） | 3 | `v0.3.0-version` |
| 6 | theme | 主题系统完善（跟随系统 + 字体） | 1 | `v0.4.0-theme` |
| 7 | tabs | 标签页 | 3 | `v0.5.0-tabs` |
| 8 | polish | 设置页、打磨与打包交付 | 5, 6, 7 | `v1.0.0-mvp` |
| 9 | optimize | UI 视觉 / 交互逻辑 / 功能增强与性能 / 缺陷修复 | 8 | `v1.1.0-optimize` |

> 阶段 4 依赖 3；阶段 5 依赖 3；阶段 6 在 1 之后可随时进行。默认**按表序串行**开发与合并（tag 自然单调）；如并行，统一按阶段号升序合并保证 tag 单调（K3）。PRD §8.2 的设计期细节在对应功能阶段**就地决策**，决策登记到《进度看板.md》待办区（G6）。

---

## 三、各阶段详述

### 阶段 0 · workflow — 项目基础设施
**目标**：建立完整的 git 仓库与 AI 工作流文档体系（仅文档，零代码）。
**关键任务**：
- `git init` + `.gitignore` + `.gitattributes`
- 《需求规格文档.md》（PRD，v1.1 已定稿）
- 《开发计划.md》（本文件）
- 《进度看板.md》（初始状态）
- 《CLAUDE.md》（AI 助手工作流手册）+ README
- `docs/` 目录；`.claude/settings.local.json` + `.claude/scheduled_tasks.json`
**交付物**：完整的 git 仓库 + 4 份核心文档 + .claude 配置。
**验收**：文档交叉引用正确；`git status` 干净；基线提交成功。
**tag**：`v0.0.1-workflow`

### 阶段 1 · scaffold — 可运行空壳
**目标**：可启动的 Electron + React 空应用，含三进程骨架、安全基线与主题 token 地基。
**关键任务**：
- 脚手架：electron-vite + `package.json` + 开发 / 构建 / 测试脚本
- 三进程骨架：main / preload / renderer
- 安全基线：`contextIsolation:true`、`nodeIntegration:false`、`sandbox:true`
- contextBridge 最小受控 API（ping 测试）
- 主题系统地基：CSS 变量 token 定义（颜色 / 间距 / 圆角 / 阴影 / 字体），浅色 / 深色两套 + 手动切换
- 工程化：ESLint + Prettier
**交付物**：`npm run dev` 启动成功的空窗口，可切换明暗主题。
**验收**：启动成功；主题切换即时生效；渲染进程无法直接访问 Node API。
**tag**：`v0.0.2-scaffold`

### 阶段 2 · workspace — 工作区设定与文件系统
**目标**：工作区设定（引导 / 持久化 / 多工作区 / 最近列表 / 更换）+ 受控 fs 读写 + 路径沙箱。
**关键任务**：
- 首次启动引导选择工作区根目录；路径持久化（localStorage），重启自动恢复
- 多工作区维护 + 最近工作区列表切换（含失效路径提示）
- 更换工作区：切换前提示保存 / 放弃未保存内容，切换后清空已打开视图（标签页交互随阶段 7 落地；阶段 2 先实现单文档切换语义）
- pathGuard 路径沙箱实现（拒绝绝对路径穿越 / `../` 穿越 / symlink realpath 逃逸）
- IPC fs 受控 API：`read_file` / `write_file` / `list_directory` / `mkdir` / `rename` / `delete`（全部经 pathGuard）
- 工作区有效性校验（启动时路径失效处理）
- 衍生数据目录 `.wr/` 规划（保持工作区目录干净，PRD §3.3/§4.3.7）
**交付物**：可选择 / 切换工作区，查看目录列表，读写文件受沙箱约束。
**验收**：路径穿越被拒并报错；多工作区切换正确；工作区目录无衍生文件散落。
**tag**：`v0.1.0-workspace`

### 阶段 3 · editor — MD 编辑器与预览
**目标**：三态编辑器（默认分屏、比例可调、可隐藏）+ 自动保存 + MD 预览 + 专注模式。
**关键任务**：
- 三态编辑器组件：默认「编辑 | 预览」分屏并排，拖拽调比例、隐藏一侧；工具栏 分屏 / 仅编辑 / 仅预览 切换
- 自动保存（输入停止 ~1s 防抖）+ 手动保存 `Ctrl+S` + 保存状态指示（已保存 / 保存中 / 未保存）
- MD 预览渲染（`marked` / `markdown-it`）：标题 / 列表 / 表格 / 代码块 / 引用 / 图片 / 链接 / 行内样式
- 图片：相对路径（基于文件所在目录）+ 远程图片（`https://`，内容呈现）；外链系统浏览器打开
- 分屏同步滚动
- 专注模式：隐藏侧边栏与顶栏，Esc / 快捷键 / 悬停边缘唤出
- 编辑器设置数据层：自动保存开关、防抖间隔、字号（UI 在阶段 8 设置页呈现）
**交付物**：可编辑 / 预览 MD 文件，自动 / 手动保存生效，专注模式可进出。
**验收**：三态切换无闪烁；防抖正确；`Ctrl+S` 即时保存；远程图片可显示；专注模式进出流畅。
**tag**：`v0.2.0-editor`

### 阶段 4 · navigation — 目录树与文件管理
**目标**：目录树展示 + 文件管理 CRUD。
**关键任务**：
- 目录树递归展示（展开时才加载子级，懒加载），展开 / 折叠
- 文件点击打开到编辑器（阶段 3 实现）；标签页交互随阶段 7 落地
- 新建 MD 文件 / 新建文件夹
- 重命名（文件 / 文件夹）：版本库按路径前缀**递归迁移**（`.wr/versions/`）
- 删除：二次确认，非空文件夹额外警告；删除时版本历史**一并清空**（递归）
- 手动刷新感知外部改动；打开中文件被外部改动时保存**覆盖前提示**
- 隐藏 `.wr/` 等软件衍生目录
**交付物**：目录树可交互，文件管理 CRUD 完整。
**验收**：CRUD 正确；删除确认弹窗正常；重命名迁移版本库正确；`.wr/` 不可见。
**tag**：`v0.2.1-navigation`

### 阶段 5 · version — 版本历史
**目标**：版本快照 + 确定性保留策略 + 行级 diff + 回滚 + 导出。
**关键任务**：
- 版本快照写入（每次保存触发；内容无变化不记版；完整内容快照）
- 集中存储 `.wr/versions/<文档相对路径>/`（`V{n}.md` 递增不重排 + `meta.json`）
- 确定性保留策略：最近 10 版全量 + 更早每 10 版留 1 里程碑 + 版本数 > 50 时精简（删非里程碑旧快照、不重排）
- 版本列表 UI（当前文件入口，展示时间与来源）
- 行级 diff 对比（任意两版）
- 回滚（产生新版本、不覆盖历史）
- 导出（系统「另存为」、默认落工作区外）
**交付物**：版本管理完整闭环。
**验收**：保存记版正确；精简策略正确（造 60 版验证）；diff 行级准确；回滚可逆；导出落工作区外。
**tag**：`v0.3.0-version`

### 阶段 6 · theme — 主题系统完善
**目标**：跟随系统 + 字体切换（含汉字）+ 主题 token 全覆盖。
**关键任务**：
- 跟随系统主题（`prefers-color-scheme` 监听），手动 / 自动切换
- 字体切换：Inter / 系统默认 / 等宽（CSS 变量 `--font-ui`）；**汉字同样应用字体样式**，等宽模式下汉字回退系统中文方案 / 统一宽度处理
- 全组件 token 覆盖率检查与补充，无硬编码颜色
**交付物**：明暗主题全组件适配，字体（含汉字）切换生效。
**验收**：跟随系统切换即时；手动覆盖后不再自动跟随；汉字字体随选择生效。
**tag**：`v0.3.1-theme`

### 阶段 7 · tabs — 标签页
**目标**：多文档标签页（MVP）；多窗口列后续迭代。
**关键任务**：
- 编辑器标签栏 + 内容区切换
- 点击文件打开 / 激活对应标签
- 关闭标签（未保存提示保存 / 放弃）
- 标签页状态持久化（localStorage，重启恢复）
**交付物**：多标签流畅切换，状态可恢复。
**验收**：切换不丢失编辑状态；关闭未保存标签有提示；重启恢复打开列表。
**tag**：`v0.4.0-tabs`

### 阶段 8 · polish — 设置页、打磨与交付
**目标**：设置页整合 + 体验打磨 + 打包发布。
**关键任务**：
- 设置页全分组：外观 / 编辑器 / 工作区 / AI（预留）/ 关于；AI 组灰色不可用 + 「未来版本」提示
- 空状态 / 错误态 / 加载态覆盖；异常处理与日志（落点 `.wr/logs/`）
- 响应式布局完善（窗口缩放自适应、侧边栏可折叠、最小尺寸下限）
- 性能优化（大目录懒加载、版本精简控磁盘、防抖保存）
- 文件编码约定落地（UTF-8，写入不含 BOM）
- 应用图标 + Windows NSIS 安装包配置
- README 与用户说明完善
**交付物**：可分发的 Windows 安装包。
**验收**：关键路径稳定；打包产物可安装运行；响应式在窗口缩放下正常。
**tag**：`v1.0.0-mvp`

### 阶段 9 · optimize — UI 视觉 / 交互逻辑 / 功能增强与性能 / 缺陷修复
**目标**：MVP 交付后的第一轮打磨——优化 UI 视觉、交互逻辑与已有功能，修复缺陷；**不引入颠覆性新功能**（多窗口 / AI / 全文搜索等见 §四）。
**关键任务**（9.2.4 失焦保存 / 9.3.6 性能专项已由用户定案取消，不列入）：
- UI 视觉优化：图标体系统一、token 一致性复查、diff 视图视觉收敛、空态 / 错误态 / 加载态统一
- 交互逻辑优化：反馈与焦点细节、快捷键体系补充、编辑联动与光标定位、新建文件流程重构
- 功能增强与显示：标签栏多开、文件树层级显示、图片粘贴资产管理、日志轮转、版本号动态化
- 缺陷修复：遇缺陷即修（不建专项清单），随优化子阶段顺手修复并登记看板
**交付物**：打磨后的 MeWork v1.1.0——UI 与交互体验提升、已知优化项逐项落地、缺陷无新回归。
**验收**：视觉与交互细节一致、无回归；各优化子阶段依《进度看板》逐项闭环；打包产物可安装运行。
**tag**：`v1.1.0-optimize`

---

## 四、后续迭代（MVP 之后，暂不排期）

> **与阶段 9 划界**：以下为颠覆性 / 新功能，不属于阶段 9（阶段 9 专注优化现有功能、不引入新功能）。后续单独排期。

- 多窗口（独立窗口共享工作区）
- AI 功能：智能归档 / AI 整理 / AI 优化 / Agent 引擎 / 模型配置（架构预留，PRD §7）
- 全文搜索
- 快速录入 Quick Capture
- 第三方改动自动检测
- 多设备同步

---

> **阶段进度与子阶段状态见《进度看板.md》**（单一事实源）；本计划只保留阶段定义、关键任务与验收标准。
